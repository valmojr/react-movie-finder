import { HomePageFrame } from "./HomePage.style";

const HomePage = () => {
    return (
        <HomePageFrame>
            <h1>Use the search bar above to search for a movie</h1>
        </HomePageFrame>
    );
}

export default HomePage;